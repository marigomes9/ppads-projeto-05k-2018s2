const
  UserRepository = require('./user-repository'),
  ListRepository = require('./list-repository'),
  TaskRepository = require('./task-repository')

module.exports = {
  ListRepository,
  UserRepository,
  TaskRepository
}
